const transition = (option) => `${option} 350ms ease-in-out`;

export const theme = {
   colors: {
      mainBlackBg: '#262624',
      mainGrayBg: '#383837',
      mainAccent: 'orange',
      mainGrayText: 'e7e7e7',
      mainWhiteText: '#fff',
      // lamp: 'orange',
   },

   transition,
};
