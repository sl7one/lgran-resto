import shape from '../../assets/img/Shape.png';
import { ShapeImg } from '../../styles/styled';

export const Shape = () => {
   return (
      <ShapeImg
         src={shape}
         alt="shape"
      ></ShapeImg>
   );
};
